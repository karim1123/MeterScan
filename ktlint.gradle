apply plugin: 'org.jlleitschuh.gradle.ktlint'

ktlint{
    android.set(true)
    ignoreFailures.set(false)
    debug.set(false)

    disabledRules = ["import-ordering"]

    reporters {
        reporter "plain"
        reporter "checkstyle"
        reporter "sarif"
    }
}
